<template>
    <t t-name="supplier_dashboard.Card" owl="1">
        <!-- <link rel="stylesheet" type="text/css" href="/supplier_management/static/src/components/card/card.css"/> -->
        <div class="card p-4 shadow-sm" t-att-class="props.color ? 'card-' + props.color : 'card-primary'">
            <div class="card-body text-center">
                <!-- Card Title -->
                <h5 class="card-title text-secondary mb-3">
                    <t t-esc="props.name"/>
                </h5>

                <!-- Card Value -->
                <div class="d-flex align-items-center justify-content-center mb-3">
                    <span class="fs-1 fw-bold text-primary" style="font-size: 3rem;">
                        <t t-esc="props.value"/>
                    </span>
                </div>
            </div>
        </div>
    </t>
</template>
